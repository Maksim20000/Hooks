1) создаем reducer
    export const Reducer = (state, action) => {
        switch (action.type) {
            default:
                return state
        }
    }

2) вызываем с помощью команды
    const [state, dispatch] = useReducer(Reducer, [начальный state])
    или
    const [state, dispatch] = useReducer(Reducer, JSON.parse(localStorage.getItem('todos')))